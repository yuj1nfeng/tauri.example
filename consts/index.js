import config from './config.js';
import fn from './fn.js';
import options from './options.js';
import events from './events.js';

export { config, fn, options, events };

export default { config, fn, options, events };
